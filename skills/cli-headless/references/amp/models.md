# `amp` — models

Provider: Amp (managed; **no** provider/model argument). The model is chosen by `--mode`.

| Mode | Use when |
|---|---|
| `rush` | fastest, simple micro-fixes |
| `smart` | default balance |
| `deep` | hardest reasoning |
| `free` | cost-free / budget runs |

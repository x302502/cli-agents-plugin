# `grok` — models

`-m, --model <MODEL>` (no provider argument). Regenerate with `grok models` (auth for the full set).

| Model | Notes |
|---|---|
| `grok-4.6` | default |
| `grok-4.5` | previous generation |

---
description: Cancel a running background CLI-agents job
argument-hint: "<job-id>"
allowed-tools: Bash
---

Run and present verbatim:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/scripts/jobs.sh" cancel $ARGUMENTS
```
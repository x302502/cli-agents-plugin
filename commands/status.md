---
description: List background CLI-agents jobs, or show one
argument-hint: "[job-id]"
allowed-tools: Bash
---

Run and present verbatim:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/scripts/jobs.sh" status $ARGUMENTS
```